# TODO Board + TinkerAgent — Implementation Progress

Plan: `docs/TODO_BOARD_PLAN.md`. Protocol: implement phase-by-phase, commit
locally per phase (never push), pause for user review between phases.

## Phase 1 — Foundations (storage + protocol + tools + REST CRUD) — DONE 2026-07-12
- [x] PG migration v16 (`todo_cards`, `todo_notes`, `todo_attachments`, `todo_note_chunks`) — applied to the live pgvector DB (schema_meta now v16)
- [x] `yuyutsava/todoboard/models.py` — versioned exchange schemas (V1)
- [x] `yuyutsava/todoboard/exchange.py` — protocol + typed exceptions + per-card workspace (`blobs/todoboard/<card_id>/`)
- [x] `yuyutsava/todoboard/store.py` — `TodoStore` ABC + Pg/Sqlite twins + default-store singleton
- [x] RoutedStore wiring in `daemon/bootstrap.py`; Pg wiring in `cli/agent_stack.py`; `TableSpec`s in `storage/routing/reconcile.py`
- [x] `yuyutsava/todoboard/tools.py` — `todo_*` family (capture scope wired into CLI deepagent + orchestrator; full scope ready for TinkerAgent)
- [x] `todo_` prefix in `ToolFilterMiddleware`; names visible in ToolRegistry catalog, schemas via `tool_search`
- [x] REST router `daemon/web/routers/todos.py` + `web/schemas/todo.py` + mount in `web/app.py` (dual `/v1/todos` + `/todos`)
- [x] Verified: 41 SQLite exchange/store/tool checks, 10 PG round-trip checks (jsonb/epoch/CASCADE), 18 REST TestClient checks, live daemon curl CRUD (create→list→get→patch→note→snapshot→404s→delete, workspace dir cleaned)
- Note: "assign this as a TODO" through a real LLM chat turn is a manual review item (tools are registered + individually exercised)

## Phase 2 — Board UI — DONE 2026-07-12
- [x] `todos` nav entry + check-square glyph in `NAV_ITEMS` (`components/layout/navIcons.jsx`); panel branch in `App.jsx` (stateless remount group)
- [x] `components/todos/TodosPanel.jsx` — four status columns (inbox/active/done/archived) with per-status accents, clickable summary cards (title, tags, pinned pin, note/attachment counts, age), create input + Add, per-card delete (confirm), 5s poll while the board is visible (agents write TODOs too)
- [x] `components/todos/TodoCardView.jsx` — expanded card: renamable title (Enter/blur commits, Esc reverts), status select, pin toggle, notes list with per-author badges + add (⌘/Ctrl+Enter) / inline edit / delete
- [x] `components/todos/shared.jsx` — STATUS_ACCENT / TagChips / PinIcon / humanAge shared by both views
- [x] `api/client.js` — listTodos/createTodo/getTodo/patchTodo/deleteTodo + note add/patch/delete on the unprefixed legacy routes; `_json` now returns null on 204 (the todo DELETEs)
- [x] Verified: `vite build` clean; live-daemon curl sequence of the exact renderer flows (create→list→rename→status+pin→note add/edit→delete note→delete card→404); real Electron run driven via Playwright — 10/10 UI checks passed (nav icon, board columns, create, open, rename, status, note add/edit/delete, card delete) with screenshots reviewed
- Note (STT dictation is Phase 5, attachment upload is Phase 4 — deliberately absent)

## Phase 3 — TinkerAgent + chat — DONE 2026-07-12
- [x] `yuyutsava/agents/tinker/` — `prompts.py` (purpose-built thinking-partner prompt: sharpen-don't-solve, small objectives, active HITL via tr_ask_user, persist insights via todo_*; reuses the CLI's TOOL DISCOVERY/ZONES blocks + host profile) and `agent.py` (`build_tinker_stack`: retrieval stores via `_build_retrieval_stores`, PG/SQLite context stores, GeneralPurposeAgent sync subagent working in the card workspace, env-gated async host attach; does NOT touch the default todo store — bootstrap's RoutedStore stands)
- [x] `build_tinker_agent(...)` — third factory in `core/engine.py`. One bundle per card (tr_* bound to the card workspace via `bind_tools(agent_name="tinker")`, card identity baked into the prompt); full yuyutsava stack: tool_search gateway + ToolFilterMiddleware, context chain (offload → compaction → transcript RAG) role="tinker", BudgetMiddleware(role="tinker") + UsageRecorder (task_id `tinker:<card>`), PermissionMiddleware on the card workspace, RetrievalInjection (memory + SkillInjector(agent="tinker") + conversation recall). Tools: todo_* FULL (author="tinker"), tr_*, vis_* (output → card `_output/`), ws_*, mem_*, ctx_*, sk_*
- [x] Multi-bundle `ConversationManager` — `self._bundles: dict[str, AgentBundle]` keyed `"master"` / `"tinker:<card_id>"`, per-agent factory map, per-key build locks, same lazy build-on-first-use; tinker sessions pinned to `thread_id = "todo:<card_id>"` (session id == thread id, so the pin doubles as the resume key); card validated via the exchange before any build; `usage_store` threaded from bootstrap
- [x] `/ws/converse?agent=tinker&card=<id>` — routes to the card bundle; hello frame carries agent/card; all existing frames (token/tool_call/image/ask/interrupt) unchanged — HITL asks flow through the existing inline-ask protocol
- [x] Tinker skills namespace — `skills/bundled/tinker/{thinking,designing,tinkering,creating}/SKILL.md`; agent-scoped via the bundled-dir mechanism (invisible to orchestrator scope), indexed into the semantic store by the stack build's SkillIndexer sync, surfaced per-turn by `SkillInjector(store, agent="tinker")`
- [x] "Think with TinkerAgent" — ChatPanel parameterized (agent/card/origin/title/placeholder/emptyHint/showVoice/showNewSession/onTurnEnd), reused not forked; `useConverse` + `ConverseClient` gained agent/card (appended as query params); TodoCardView "✦ Tinker" header toggle splits the view into notes | chat, `onTurnEnd` refreshes the notes pane, `resumeId=todo:<card>` hydrates past turns on reopen. Voice toggle deliberately absent (Phase 5)
- [x] Verified: 29 standalone python checks (prompt render, skills namespace + agent scoping, compiled graph with todo_*/tr_*/vis_*/ws_*/tool_search registered, manager key/factory/validation logic); vite build clean; live WS smoke (fresh open → tool_search → tr_ask_user ask answered inline → todo_add_note → reconnect resuming=True with memory of the agreed scope); real Electron run via Playwright — board → card → ✦ Tinker → hydrated history → clarifying Question box answered inline → second tinker note appeared on the card (screenshots reviewed) → close/reopen resumed the thread; notes confirmed via REST with author="tinker"
- Note: demo card `tdo_01KXB245YKADRSYZK40TS9Q483` ("Phase-3 tinker e2e card") left on the board for review

## Phase 4 — Attachments + artifact blocks — DONE 2026-07-12
- [x] `yuyutsava/todoboard/artifacts.py` — pluggable artifact-block registry: `ArtifactBlock` (name, storage kind, mimes, upload_mimes, validator, optional `generate(spec, out_dir)` delegating to `visuals.render`); dispatch by (kind, mime). Storage `kind` keeps the closed V1 vocabulary (versioned schema + DB CHECK); new blocks (Phase 7 JSX/audio) ride on `artifact`/`file` kinds via mime — proven in checks by registering an `audio` block with zero core edits. `.md → text/markdown` registered (Python <3.13 gap)
- [x] `exchange.attach()` now dispatches validation through the registry (lazy import breaks the exceptions cycle); validators run off-loop and infer missing mimes from the file suffix
- [x] Multipart REST (first in the codebase) in `routers/todos.py`: `POST /todos/{id}/attachments` (UploadFile + optional title/kind Form fields; 50 MB cap enforced mid-stream → 413; registry mime allowlist → 415; kind inferred from mime; streamed 1 MB chunks into the card workspace; rollback covenant: file first, row second, unlink on ANY failure incl. 413), `GET .../attachments/{aid}` (FileResponse à la visuals.py, `?download=true` sets Content-Disposition), `DELETE` (exchange unlinks workspace-resident files). Auto dual-mounted `/v1/todos` + `/todos`
- [x] `list_card_ids` on `TodoStore` ABC + both twins + exchange (RoutedStore proxies via `__getattr__`)
- [x] Orphan-dir sweep in `UnifiedSweeper`: removes `blobs/todoboard/<dir>` with no card row; NO TTL (durable user data); 1h mtime grace covers the exchange's mkdir-before-row ordering; card ids via the exchange only; sweep skipped while storage degraded (buffer card list is partial) or when the id listing fails; wired in bootstrap with `get_default_exchange()` + `storage_health`
- [x] Frontend `components/todos/artifactBlocks/` — ordered `matches(att)` registry (`index.js`): DiagramBlock, ImageBlock, TextBlock (fetches + previews text, source only — never DOM-injected), LinkBlock; unknown kinds (artifact/video/…) fall back to DownloadTile (name, kind·mime·size, Download link)
- [x] TodoCardView: Attachments gallery in the notes column (coexists with the Phase-3 notes|chat split) — drag-drop zone + hidden picker → `uploadTodoAttachment`, per-tile kind badge/title/age/Delete (confirm), auto-fill grid; `client.js` gained uploadTodoAttachment (FormData, surfaces 413/415 detail), todoAttachmentUrl, deleteTodoAttachment
- [x] Verified: 68 standalone python checks (registry resolution/allowlist/kind-inference/pluggability, exchange dispatch on SQLite, TestClient multipart incl. 413-no-partial-file + row-failure-rollback + cross-card 404s + filename collision, orphan sweep incl. degraded/failed-listing skips); PG round-trip via live daemon curl (upload png/md, GET bytes-identical, download disposition, 415, /v1 mount) + exchange-over-PG artifact attach + `list_card_ids`; vite build clean; real Electron runs via Playwright — gallery blocks (image decoded, md text preview, artifact→download tile), picker + drag-drop uploads, targeted attachment delete, "✦ Tinker → vis_diagram (graphviz) → todo_attach_artifact kind=diagram" landed on the card and renders as a DIAGRAM tile (screenshots reviewed)
- Note: e2e card `tdo_01KXBHWGQAE9Y9FBZTAM56GENX` ("Phase-4 attachments e2e card", 4 attachments incl. the tinker diagram) left on the board for review; the Phase-3 demo card was deleted by the user mid-verification

## Phase 5 — Voice + STT dictation — DONE 2026-07-12
- [x] Voice mode in the card view — the mic toggle enabled on TodoCardView's embedded tinker ChatPanel (dropped `showVoice={false}`); nothing new server-side: the panel's single WS already carries agent/card, so the existing voice frames (VAD → transcript → spoken turn → audio_chunk TTS, half-duplex mic) hit the card-bound TinkerAgent unchanged
- [x] `?mode=dictate` on `/ws/converse` — transcribe-only loop reusing the exact voice plumbing (VoicePipeline VAD → STT), no session opened, no agent turn ever runs; utterances transcribed strictly in capture order by a single worker, each sent as its own `transcript` frame (same low-confidence gate as chat); `audio_end` flushes the VAD tail and acks `dictate_done`; one dictation per connection
- [x] `hooks/useDictation.js` — reuses MicCapture + ConverseClient (which gained a `mode` option); start() dials + opens the mic, stop() releases the mic → `audio_end` → disconnects on `dictate_done` (10s timeout so a wedged socket can't pin the UI); teardown is idempotent, suppresses auto-reconnect, never leaves a hot mic
- [x] 🎙 dictate button in the note composer — transcripts append into the draft textarea for the user to edit; saving is always the explicit + Add note (never auto-submits)
- [x] Pause/play for spoken replies (user request) — AudioPlayer `pause()/resume()` (context suspend + a user-paused flag so `ensureContext`'s auto-resume on each streamed chunk can't un-pause; `stop()` clears the flag or all future audio would stay muted); `useConverse` gained `paused`/`togglePause`; round ▶/⏸ control on ChatPanel bubbles that carry audio (in-session PCM chunks or a persisted `audio_url`) — ▶ plays/resumes, ⏸ pauses in place with position held
- [x] Mixed text+voice history fix — `/sessions/{id}/messages` returned the voice surface only for `origin == "voice"`, so a reopened tinker card lost its clips: transcript rows now get voice-store clips reattached (matched by the turn's final text, each clip used once, in order); `useConverse` hydration uses the server's `audio_url` verbatim (it carries the voice-store seq — rebuilding from the transcript row's seq 404'd); dead `sessionAudioUrl` helper removed
- [x] Verified: vite build clean; synthetic WS dictation against the live daemon (`say`-generated WAV → exact transcript, zero agent-turn frames, `dictate_done`); synthetic WS voice-tinker on a fresh test card (transcript → tool_search → todo_add_note author="tinker" → TTS audio_chunks → turn_end; note confirmed via REST; reopening hydrated the spoken turn); Playwright Electron runs — dictate + chat mic buttons render and go hot (fake media device), dictate stop→done round-trip, and the full play→pause→hold-position→resume→drain cycle on persisted clips (screenshots reviewed); live mic test by the user (voice-tinkering a card + dictating a note) confirmed working
- Note: test card `tdo_01KXBMFX73E5G31W45RCD9Z3NY` ("Phase-5 voice e2e card (claude test)") left on the board for review

## Phase 6 — Master delegation + MCP + note recall — DONE 2026-07-13
- [x] `yuyutsava/agents/tinker/subagent.py` — `TinkerSubAgent(BaseSubAgent)` + `make_tinker_subagent()`: the **`tinker-bg` async subagent** of the master. One board-scoped graph (NOT per-card): `start_async_task` passes only free text to a graph compiled once at host boot, so a per-card compile is unreachable and the run lives on the host's own thread/checkpointer — it can't (and shouldn't) join the interactive `todo:<card_id>` thread. Card semantics are kept at the tool level instead: card id travels in the task description (the spec description instructs the master), tr_* is zone-bound to the board root (`blobs/todoboard/` — sandbox deliberately OUTSIDE it, the orphan sweep would eat an in-board sandbox), and all durable output lands ON the card via full-scope todo_* (author="tinker"). ws_* full set (researcher rule), vis_*, mem_*, sk_*, MCP via `tools_for("tinker")`
- [x] Registration: daemon bootstrap builds `bg_subagent_list = subagents + [tinker_sub]` → async host graphs (`graphs=[…,'tinker']`), orchestrator `deps.async_subagents`, triage capabilities block; `cli/agent_stack.py` adds it to the conversational master's `async_subagents` (so master chat/voice sees `tinker-bg`; attaches to the daemon host first-come-wins). Async-only by design — NOT in the sync subagents dict
- [x] Completion-loop gap closed for conversations: only the orchestrator loop recorded `start_async_task` launches, so a bg task launched from `/ws/converse` was never correlated → no wake. `ConversationManager.record_async_launch()` (mirror of `OrchestratorLoop._record_async_launch`) + hook in the converse router's event sink; `launch_index` threaded from bootstrap. Asks from bg runs ride the existing `AsyncTaskHealthWatcher → ChannelRouter.post_ask` plumbing untouched
- [x] Latent wake-killer fixed in `orchestrator_loop.py`: wake-ups/resumes/direct tasks carry `proposal_id=""`, and `put_decision("")` violates `decisions_proposal_fk` on Postgres AFTER the turn already streamed (mark_done never ran). Decisions now recorded only for proposal-born tasks — found live when the first real wake crashed on it
- [x] MCP into the tinker: `build_tinker_agent(mcp_tools=…)` registers user-configured MCP server tools exactly like the orchestrator master (`mcp_manager.tools_for("tinker")`, unprefixed names model-visible, rest via tool_search); threaded `bootstrap → ConversationManager(mcp_manager=…) → build_tinker_stack → build_tinker_agent`; the bg tinker gets the same via BaseSubAgent's built-in mcp_manager support
- [x] `todo_note_chunks` recall LIVE — `yuyutsava/todoboard/recall.py`: `TodoNoteIndex` on the shared `PgVectorTable`/`PgVectorSearch` engine (extra_cols card_id+note_id in hit payloads). Embed-on-write hooks INSIDE the exchange (`add_note` → schedule, `update_note` → replace; fire-and-forget, degraded storage skips — never crashes a write); boot backfill `sync(exchange)` reads the board through the exchange only + NULL-vector re-embed (`retrieval` backfill); deletes need no hook (chunks CASCADE note→chunks, card→notes→chunks). Default-index singleton mirrors the store pattern; daemon wires + syncs at boot, CLI wires when it owns a pool (no boot sync — stays light)
- [x] Recall surfaced both ways the plan intends: `exchange.search_notes()` + new **`todo_recall`** tool in the CAPTURE scope (any master agent learns about TODOs through the protocol; keyword fallback when the embedder is down, graceful message when no index) and `TodoNoteInjector` (board-wide, cross-card by design) in `build_tinker_agent`'s per-turn RetrievalInjection chain
- [x] Verified: 37 standalone logic checks (index inert without pool, exchange hooks add/replace, search_notes validation, todo_recall rendering both scopes, TinkerSubAgent spec/tools/zones/prompt, compiled async graph, capabilities advertise tinker-bg, engine compiles with mcp_tools+note_index, launch recording filters); 17 live-PG checks (embed-on-write via exchange, idempotent re-index, cosine ranking + card scoping, NULL-row outage → keyword fallback → backfill repair, update replaces chunks, boot sync via exchange, note/card CASCADE); daemon boot (backfill indexed 5 real notes, re-embedded on next boot after ollama came up, host graphs include tinker); live e2e over synthetic WS master: "delegate tinkering on card X in background" → `start_async_task(tinker-bg)` with the card id in the description → bg run researched + wrote tinker-authored notes on the card (confirmed via REST) → watcher completion → `OrchestratorTask(kind=subagent_completed)` wake ran ON the launching conversation's thread (task-registry row on the parent thread, marked done with the summary)
- Note: e2e card `tdo_01KXCQAZXFFJ90XP0P4YQCPK28` ("Phase-6 delegation e2e card (claude test)") left on the board for review — user card untouched

## Phase 7 — Advanced artifact blocks — DONE 2026-07-13
- [x] `yuyutsava/todoboard/block_jsx.py` — JSX sandbox block, backend half: rides the closed V1 `artifact` kind via `text/jsx` (`.jsx` mimetypes gap registered, same fix as `.md`), uploadable through the existing multipart allowlist. One module + one `register_block` entry in `artifacts.py`'s registration section — zero edits to exchange/store/router
- [x] `yuyutsava/todoboard/block_audio.py` — audio block: rides `file` via `audio/*` mimes (upload allowlist wav/mp3/mp4/aac/ogg/webm/flac), `generate(spec, out_dir)` delegates to the voice interface's synth path (`synthesize_pcm` over `tts_from_env()`: Piper, or zero-config macOS `say`/pyttsx3 fallback) and writes a mono 16-bit WAV (write_voice_wav pattern) into the card workspace
- [x] Frontend `SandboxBlock.jsx` — html/jsx artifacts render inside a hardened sandbox: `sandbox="allow-scripts"` srcDoc iframe (opaque origin, no same-origin/top-nav/popups/forms) + injected `default-src 'none'` CSP meta (inline script/style + data: assets only — NO network of any kind); JSX transpiled locally by lazy-chunked `@babel/standalone` (new dep) with classic runtime + jsx-runtime shim, run against the app's inlined React 18 UMDs via a tiny CommonJS shim (contract: export default / export App / window.App / self-mount into #root); run|source toggle; registered BEFORE TextBlock so the ordered registry overrides text/html source-preview with live render — TextBlock untouched
- [x] Frontend `AudioBlock.jsx` — any `audio/*` attachment gets the ChatPanel-style round ▶/⏸ control through the shared `audioPlayer` singleton (`playUrl` fetch+decode — an `<audio>` tag would be CSP-blocked, media-src isn't allowlisted; `pause()/resume()` hold position via the user-paused flag; drain-poll pattern from useConverse; stops on unmount)
- [x] `index.js` registry: two new ordered entries (sandbox before text, audio after link) — zero edits to TodoCardView or other blocks
- [x] **`todo_generate_artifact` dispatcher tool** (user-approved core edit — the one Phase-4 left missing: nothing dispatched `ArtifactBlock.generate` from the agent surface): kind-agnostic `todo_generate_artifact(card_id, block, spec, title)` in the FULL scope only — resolves the block by name from the registry, runs its generator off-loop into the card workspace, attaches via the exchange with `meta={source, block, author}`; JSON-string specs parsed (models stringify nested objects — found live when qwen's 4 attempts all bounced off the strict dict schema); structured errors for unknown/non-generative blocks and generator failures; future generative blocks need zero edits here
- [x] Verified: 49 standalone checks (registry resolution incl. v1 blocks untouched, allowlist/kind-inference, validators incl. audio-family pin, real `say` TTS through the generate hook → playable mono 16-bit WAV, exchange dispatch on SQLite, minimal-app TestClient multipart incl. octet-stream→suffix inference + 415, dispatcher tool scope/happy-path/error-paths/string-spec); vite build clean (babel in a lazy chunk loaded only when a JSX artifact is on screen); live-PG e2e: .jsx/.html/.wav uploads landed with right kinds/mimes, tinker over `/ws/converse?agent=tinker&card=` called `todo_generate_artifact(block=audio)` → 4.18s WAV voice note attached `author=tinker`; Playwright Electron run — 17/17 UI checks (jsx counter interactive IN the sandbox, html inline script ran, **CSP proof: in-frame fetch to the daemon blocked by `default-src 'none'`**, both audio tiles ▶→playing→⏸ paused-in-place→resume, source toggle) with screenshots reviewed
- Note: e2e card `tdo_01KXD7T1SC5ECY1XKYQJTZM02C` ("Phase-7 artifact blocks e2e card (claude test)", 3 uploads + the tinker-generated voice note) left on the board for review — user cards untouched
