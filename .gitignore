# Environment variables
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python

# Virtual environments
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# Build & distribution
dist/
build/
*.egg-info/
*.egg
.eggs/
MANIFEST

# uv
.python-version

# Testing & coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
nosetests.xml
coverage.xml
*.cover

# Type checking & linting
.mypy_cache/
.ruff_cache/
.dmypy.json
dmypy.json

# Logs
*.log
streamlit.log

# Jupyter
.ipynb_checkpoints/
*.ipynb

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Claude Code
.claude/

# Sandbox & runtime workspace
workspace/
sandbox/
playground/

# deepagents on-disk scratch (durable copies live in the DB:
# artifacts + transcript_messages tables). CLI deletes these on exit;
# the daemon TTL-sweeps them — never commit them.
large_tool_results/
conversation_history/

# LangGraph in-proc dev-server state (AsyncSubagentHost / langgraph_api).
# Regenerated on every run and rewritten continuously while the daemon is up,
# so committing it produces a ~660KB..5MB binary diff per commit that conflicts
# on every branch. It also carries real run history (thread/run ids, Human/AI
# message payloads, token usage) — never commit them.
.langgraph_api/

# Diagrams (generated artifacts)
diagrams/

content/


_sandbox
.claude
.venv
.vscode
content
.env.gcloud/
.gcloud/
